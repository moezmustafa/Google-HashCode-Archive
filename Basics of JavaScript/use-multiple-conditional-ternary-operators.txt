//using multiple Ternary Operation inside a single function

// to use multiple , conditions the syntax is :

// ( condition 1 || the if statement) ? " enter what to do here" : (condition 2 || else-if) ? " Enter what to do here" : "Default goes at the end ";

function checkSign(num) {
   
   return (num > 0) ? "positive" : (num < 0)? "negative" : "zero";
}

checkSign(10);